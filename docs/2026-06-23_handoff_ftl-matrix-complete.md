# Scope Android - Handoff (2026-06-23): FTL matrix done, Play prep next

You boot with zero memory of this session. This is your boot sequence.
Supersedes `docs/2026-06-16_handoff_oem-spectrum-and-play.md`.

Scope is a multi-target audio visualiser (phone + TV + desktop). One codebase;
Android is a Capacitor WebView APK. FTL instrumentation suite validates the
capture/analysis/render pipeline across OEMs. All FTL audit work is now
committed and pushed; the next frontier is Google Play upload prep.

## Next up (do these in order)

1. **Google Play upload prep.** Build a signed `bundleRelease`, complete the
   store listing (screenshots, content rating, description), and submit through
   the Play Console. Privacy policy is at `docs/privacy-policy.md` (tracked).
   Confirm scope with the user before starting - this is the first Play upload
   and has its own checklist (see Load-bearing facts below).

## Skills to load, and when

- `systematic-debugging` - if any build or upload step fails (confirm root
  cause before fixing).
- `verification-before-completion` - "build succeeded" != "uploaded correctly";
  verify each step in the actual Play Console UI.

## Read first (in this order)

1. `docs/architecture.md` - one-codebase / three-targets overview; relevant
   before touching build config.
2. `docs/build-signed.md` - release build procedure (tracked, in the repo).
3. `android/app/build.gradle` `defaultConfig` - versionCode (currently 1),
   versionName, and signing config wiring.
4. `android/gradle.properties` - keystore path and passwords (local-only,
   gitignored).

## Task ledger

- FTL virtual matrix (MediumPhone.arm v34/35/36) AudioCaptureTest - **COMMITTED** `b1b218b` (pushed).
- FTL physical matrix (dm1q/CPH2449/RE58C2/a35x) AudioCaptureTest - **COMMITTED** `b1b218b` (pushed).
- TvJourneyTest added for Sabrina, excluded from FTL - **COMMITTED** `b1b218b` (pushed).
- FTL v36 MediaProjection tap fix (77% width not 50%) - **COMMITTED** `b1b218b` (pushed).
- Google Play upload prep - **NOT DONE**.

First act: copy this ledger into your task tracker (TodoWrite where available)
and keep it current as you work.

## Load-bearing facts (confirmed - do not re-derive)

**FTL audit (complete, committed b1b218b):**
- All 7 FTL devices pass AudioCaptureTest: v34/35/36 virtual (1 test each,
  @RequiresDevice skips mic test) + dm1q/CPH2449/RE58C2/a35x physical (2 tests
  each). Both virtual and physical matrices are pushed to origin/main.
- FTL v36 virtual: UiAutomator cannot see the MediaProjectionPermissionActivity
  window in its accessibility tree. Coordinate tap at (77% width, 67% height)
  is the fallback. Tapping center (50%) lands between Cancel and Share screen.
  This is documented with a comment in `JourneySupport.kt:startSystemCapture()`.
- TvJourneyTest skips on non-TV via `Assume.assumeTrue(UiModeManager ==
  UI_MODE_TYPE_TELEVISION)`. Excluded from `run-ftl-instr.sh` TEST_TARGETS
  (FTL has no TV devices). Run against Sabrina via `run-instr-local.sh`.
- One clean capture per `am instrument` process - only the FIRST
  system-audio capture in a process gets a clean mix; subsequent captures read
  silence. This is why AudioCaptureTest runs first in the class list and why
  only one captured-audio assertion exists per test class.
- Composite tone `tone-bands-100-1500-9000hz.wav` (100+1500+9000 Hz) is a
  BUILD ARTIFACT, never committed. Generated by `tools/audit/gen-test-tones.sh`
  (requires `ffmpeg`). `run-ftl-instr.sh` and `run-instr-local.sh` call it
  before each build.

**Google Play upload (NOT yet started):**
- versionCode is held at `1` in `android/app/build.gradle`. Do NOT increment
  it without explicit, in-this-request user authorisation. For a first Play
  upload there is no prior APK to exceed, so 1 is correct.
- Release keystore: `/home/alexie/.android/scope-release.keystore`. Signing
  config is in `android/gradle.properties` (local-only, gitignored).
- Use `./gradlew :app:bundleRelease` (AAB for Play), not `assembleRelease`.
- Privacy policy URL must be live and reachable before submission.

## Devices

- **Pixel Tablet `tangorpro`**: PRIMARY local test device; wireless-adb via
  mDNS (`ADB_MDNS_OPENSCREEN=1 adb mdns services`). See memory
  `reference_device_identifiers` for full device table and serials.
- **Sabrina (Google TV)**: for TvJourneyTest. Connect via mDNS same way.
- **FTL**: project `scope-audit-202606b` (Spark). Virtual 10/day, physical
  5/day. Authoritative quota: `bash tools/audit/ftl-preflight.sh --report`.

## Discipline (obey project + global CLAUDE.md)

- `code-reviewer` dispatch before every `git commit` AND every device
  build+install (lint+typecheck+tests green first).
- One commit per plan at plan-end; explicit pathspec only; main agent only;
  NEVER push without an explicit per-request user ask.
- NEVER change `versionCode` without explicit user authorisation in that
  specific request.
- No em dashes anywhere. Times in AEST. Write `$TMP` not `/tmp` in paths.
- `docs/*` is gitignored except `docs/privacy-policy.md`,
  `docs/build-signed.md`, `docs/SECURITY.md`. This handoff is LOCAL-ONLY.
